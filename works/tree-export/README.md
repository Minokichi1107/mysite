## Usage

1. フォルダ構造を一覧化したいディレクトリで  
   `tree_export.bat` を実行します。

2. カレントディレクトリの構造が  
   `tree.txt` として出力されます。

※ Windows 標準の `tree` コマンドを使用しています。

## Output example

.
├─ docs
├─ src
│  ├─ main
│  └─ test
└─ README.md

（※ 実際の出力は環境により異なります）
